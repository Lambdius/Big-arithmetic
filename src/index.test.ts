"use strict";



// import { expect } from "chai";
import { describe, it } from "mocha";


// import { expressionParser } from "./0_parser";
// import { add } from "./1_addition";
// import { sub } from "./2_subtraction";
// import { mul } from "./3_multiplication";
// import { div } from "./4_division";



describe("Big arithmetic tests:", () => {

    it("Division tests", () => {

        for ( let i = 0; i < 100; i++ )
        {
            // expect( expressionParser( [ "1", "+", "2" ] ) );
        }

    });

    it("Addition tests", () => {

        for ( let i = 0; i < 100; i++ )
        {
            // expect( add( "1", "2" ) );
        }

    });

    it("Subtraction tests", () => {

        for ( let i = 0; i < 100; i++ )
        {
            // expect( sub( "1", "2" ) );
        }

    });

    it("Multiplication tests", () => {

        for ( let i = 0; i < 100; i++ )
        {
            // expect( mul( "1", "2" ) );
        }

    });

    it("Division tests", () => {

        for ( let i = 0; i < 100; i++ )
        {
            // expect( div( "1", "2" ) );
        }

    });

});
